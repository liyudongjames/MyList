<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="170dp">

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@id/interrelatied_recyclerview_item_imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        app:placeholderImageScaleType="fitCenter"
        app:placeholderImage = "@drawable/home_article_img_twill"
        app:fadeDuration="1000" />

    <TextView
        android:id="@id/interrelatied_recyclerview_item_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="测试，测试，测试，测试，测试"
        android:textColor="@android:color/white"
        android:maxLines="2"
        android:layout_gravity="bottom"/>

</FrameLayout>